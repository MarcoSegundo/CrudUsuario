CREATE TABLE TELEFONE (
CD_TELEFONE INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1, INCREMENT BY 1) PRIMARY KEY,
DDD_FONE VARCHAR(3) NOT NULL,
NUM_FONE VARCHAR(9) NOT NULL,
TIPO VARCHAR(1) NOT NULL,
CD_USUARIO INTEGER NOT NULL,
FOREIGN KEY (CD_USUARIO) REFERENCES USUARIO(CD_USUARIO)
);